import { GuildMember } from 'discord.js';

const event = {
  name: 'guildMemberUpdate',
  once: false,
  execute: (oldMember: GuildMember, newMember: GuildMember) => console.log('guildMemberUpdate', oldMember, newMember),
};
export { event };
